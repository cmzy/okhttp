apply plugin: 'com.github.johnrengelman.shadow'

tasks.withType(JavaCompile) {
  options.warnings = false
}

jar {
  manifest {
    attributes 'Main-Class': 'mOkhttp3.sample.SampleServer'
  }
}

dependencies {
  implementation project(':mockwebserver')
}

shadowJar {
  mergeServiceFiles()
}
